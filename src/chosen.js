(function (angular) {
    'use strict';

    angular.module('chosen', []).directive('chosen', function () {
        return {
            restrict: 'A',
            compile: function (element, attrs) {
                return function (scope, element, attrs) {

                };
            }
        };
    });

}(angular));
